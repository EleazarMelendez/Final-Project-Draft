import {nlpApplet} 


for (const [id, value] of Object.entries(result)) {
  const { result, error } = await supabase
    .from('ParsedArticles')
    .update({ tfidf_score: value })
    .match({ id: id });
  
  if (error) {
    console.error(error);
  } else {
    console.log(`Updated ${updatedData.length} row(s) for id ${id}`);
  }